#include <stdio.h>

/**
 * main - print all the args that the prog recieves
 * @argc:input
 * @argv:input
 * Return:always 0 success
 */

int main(int argc, char const *argv[])
{
	int = 0;

	while (argc--)
	{
		printf("%s\n", argv[i]);
		i++;
	}
	return (0);
}
