#include "main.h"

/**
 * add - function that add 2 integers
 * @num1 : parameter for addition op
 * @num2 : parameter to add
 * Return: the result
 */

int add(int num1, int num2)
{
	int sum;

	sum = num1 + num2;
	return (sum);
}
